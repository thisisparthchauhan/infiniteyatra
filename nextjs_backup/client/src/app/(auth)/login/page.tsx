'use client';

export default function LoginPage() {
    return (
        <div className="min-h-screen bg-[#050505] text-white flex items-center justify-center">
            {/* TODO: Migrate Login.jsx here.
                Replace:
                  - useNavigate → useRouter from 'next/navigation'
                  - Link to= → Link href=
                  - import from '../context/AuthContext' → import from '@/context/AuthContext'
            */}
            <p className="text-white/60">Login — migrate Login.jsx here</p>
        </div>
    );
}
